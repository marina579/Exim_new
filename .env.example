# Google Cloud Storage Configuration for PDF Quotations
GCS_BUCKET_NAME=marineco-quotations
GCS_PROJECT_ID=your-project-id
GCS_CREDENTIALS_PATH=./gcs-credentials.json

# Optional: Change quotation expiry days (default: 30)
# QUOTATION_EXPIRY_DAYS=30
