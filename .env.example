# Google Cloud Storage Configuration for PDF Quotations
GCS_BUCKET_NAME=marineco-quotations
GCS_PROJECT_ID=wise-hub-483204-p4

# For Railway: Paste your service account JSON key as a single line
# GCS_CREDENTIALS_JSON={"type":"service_account","project_id":"..."}

# For Local: Use Application Default Credentials (ADC)
# Run: gcloud auth application-default login
# No need to set GCS_CREDENTIALS_JSON locally

# n8n Webhook for Sending WhatsApp Messages
N8N_WEBHOOK_URL=https://n8n.srv1243049.hstgr.cloud/webhook/0a82c35c-0bb2-4d12-934f-02411715e85a

# Database (if using PostgreSQL for production)
# DATABASE_URL=postgresql://user:password@host:port/database
